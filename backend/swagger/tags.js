const tags = [
    {
      name: "User",
      description: "Endpoints related to user authentication (login, signup)."
    },
    {
      name: "Books",
      description: "Endpoints for managing books (CRUD operations)."
    }
  ];
  
  module.exports = tags;